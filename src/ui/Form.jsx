import { styled } from 'styled-components';

const Form = styled.form`
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 1.2rem;

  & p {
    text-align: center;
  }
`;

export default Form;
